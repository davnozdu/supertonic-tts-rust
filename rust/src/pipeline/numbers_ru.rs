//! Russian number-to-words normaliser.
//!
//! Stub — full integer / decimal / percent / range handling will be
//! ported from the existing Kotlin `RussianNumberNormalizer` next.

#![allow(dead_code)]

/// No-op for now. Returns input unchanged.
pub fn normalize(text: &str) -> String {
    text.to_string()
}
